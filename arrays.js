// arrays collection of strings text & number 
let name = ['Edward Robertson', 24, 'liberia', 'Black'];

console.log(name);

// arrays of object

let myAnimals = [{species: 'Monkey', Kingdom: 'Phylum', Domain: 'Eukarya'},
{species: 'Dog', Kingdom: 'Phylum', Domain: 'Eukarya'}];

// add a new element to the array
name.push('Edward');

// display the first data with the index 0 = Edward Robertson
console.log(name[0]);

// array method
console.log(name[1]);


